"""Write tools this fork adds to the upstream NetBox MCP server.

Everything the fork adds lives under this package, so a `git merge upstream/<tag>`
touches no file the fork owns. See FORK.md.
"""

__all__ = ["write_tools"]
